\usepackage[english]{babel}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}

% Modern looking fonts.
\usepackage{lmodern}

% Provides generic commands \degree, \celsius, \perthousand, \micro and \ohm.
\usepackage{gensymb}
% Similar.
%\usepackage{textcomp}

% Upright Greek letters as text symbols, e.g. \textbeta.
\usepackage{textgreek}

% AMS-LATEX bundle (collection of packages and classes for math typesetting.
% AMS - American Mathematical Society
\usepackage{amsmath}
\usepackage{amssymb}
% "amsfonts" is a collection of fonts that extends the Computer Modern family
% with additional symbols and alphabets intended for use in mathematical
% contexts. Full LaTeX support is provided by the included "amssymb" and
% scripts.
%\usepackage{amsfonts}
%\usepackage{amsrefs}

% To get IEEEeqnarray environment, which should be use almost always when
% multi line equation are required.
\usepackage{IEEEtrantools}


% Improves the interface for defining floating objects. Introduces boxed float,
% the ruled float and plain float.
\usepackage{float}

% Easy flexible interface to customize page layout.
\usepackage[margin=2cm,bottom=3cm,foot=1.5cm]{geometry}
\frenchspacing

\usepackage{fontspec}
\setmainfont{Calibri}

% Required for including images.
\usepackage{graphicx} 
\graphicspath{{figures/}} % Directory in which figures are stored


% Builds upon "color" package. Adds color tints, shades, tones and mixes.
% Colors can be mixed like \color{red!30!green!40!blue}.
\usepackage{xcolor}

% Cross-references are links.
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    filecolor=brown,      
    urlcolor=darkgreen,
    }

% Commands.

% Environments.

% Colors.

% Counters.

% Lengths.
